export interface Entity {
  idEntidad: number;
  tipoDocumento: any; // Update the type according to your needs
  numeroDocumento: string;
  razonSocial: string;
  nombreComercial: string;
  tipoContribuyente: any; // Update the type according to your needs
  direccion: string;
  telefono: string;
  estado: boolean;
}
